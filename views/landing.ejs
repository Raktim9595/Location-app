<!DOCTYPE html>
<html>
    <head>
        <title>Tours || for adventorous people</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/landing.css">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
    </head>
    <body>
    
    <div class="container">
        <div class="container-fluid">
            <% if (errorMessage && errorMessage.length> 0) { %>
                <div class="alert alert-danger mb-1 alert-dismissible fade show" role="alert">
                    <%= errorMessage %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
                <% } else if ( successMessage && successMessage.length> 0 ) { %>
                    <div class="alert alert-success mb-1 alert-dismissible fade show" role="alert">
                        <%= successMessage %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                <% } %>
        </div>
    </div>
    
    <div id="landing-header">
 		<h1>Explore Nepal</h1>
        <span class="landing__text">
            Simple minimal website for travellers and nature enthusiasts who want to explore the small and beautful nation Nepal.
        </span>
		<a href="/campgrounds" class="btn btn-lg btn-success">View All Locations</a>
    </div>
    
    <ul class="slideshow">
      <li class="img-fluid"></li>
      <li></li>
      <li></li>
      <li></li>
      <li></li>
    </ul>